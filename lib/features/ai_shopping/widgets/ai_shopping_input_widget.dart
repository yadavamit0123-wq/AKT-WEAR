import 'package:flutter/material.dart';

class AiShoppingInputWidget extends StatelessWidget {
  const AiShoppingInputWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox.shrink();
  }
}
